<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Enter PIN</title>
  <style>
    body {
      background: #f0f4f8;
      font-family: 'Segoe UI', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    .pin-container {
      background: white;
      padding: 2rem;
      border-radius: 12px;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
      text-align: center;
      max-width: 400px;
      width: 100%;
    }

    .pin-container h2 {
      margin-bottom: 1rem;
      color: #333;
    }

    .input-group {
      margin-bottom: 1.5rem;
    }

    input[type="number"] {
      width: 100%;
      padding: 0.75rem;
      font-size: 1.1rem;
      border: 1px solid #ccc;
      border-radius: 8px;
      text-align: center;
    }

    button {
      background-color: #4a90e2;
      color: white;
      border: none;
      padding: 0.75rem 1.5rem;
      font-size: 1rem;
      border-radius: 8px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: #357bd8;
    }
  </style>
</head>
<body>

  <div class="pin-container">
    <h2>A PIN has been sent to your Gmail.</h2>
    <form action="/user/verify_pin" method="POST">
      <div class="input-group">
        <input type="text" name="gmail" value="<%=gmail%>">

        <input type="number" name="pin" placeholder="Enter 6-digit PIN" required min="100000" max="999999">
      </div>
      <button type="submit">Verify</button>
    </form>
  </div>

</body>
</html>
